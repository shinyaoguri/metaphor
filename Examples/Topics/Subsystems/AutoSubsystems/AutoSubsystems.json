{
  "name": "AutoSubsystems",
  "title": "Auto Subsystems (lifecycle)",
  "level": "Intermediate",
  "description": "Registers a Physics2D world as a SketchSubsystem with AutoSubsystemManager so its per-frame step() is driven automatically — draw() only renders. Demonstrates the opt-in subsystem lifecycle (audio/video/physics also conform).",
  "featured": [
    "AutoSubsystemManager",
    "SketchSubsystem",
    "Physics2D",
    "PluginFactory"
  ]
}
