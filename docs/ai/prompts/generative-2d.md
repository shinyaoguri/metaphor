# Generative 2D Sketch Prompt

Write a complete metaphor `App.swift` sketch.

Goal:
- Create [describe the visual: e.g. a calm field of orbiting points, flowing
  ribbons, geometric pattern, kinetic poster].

Constraints:
- Use `import metaphor`.
- Canvas: [width] x [height].
- Keep `draw()` realtime safe.
- Put long-lived state in properties.
- Initialize arrays/resources in `setup()`.
- Use `time`, `deltaTime`, and `frameCount` for animation.
- Use only APIs present in `llms-sketch.txt` or `llms.txt`.

Style:
- Palette: [describe colors].
- Motion: [smooth / chaotic / looping / mouse-reactive].
- Interaction: [none / mouse / keyboard].

Return:
- The complete Swift file.
- A short note listing controls and any assumptions.
